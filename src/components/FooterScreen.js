import React from "react";
import styled from "styled-components";

const FooterScreen = () => {
  return (
    <Container>
      <Wrapper>Footer Screen</Wrapper>
    </Container>
  );
};

export default FooterScreen;

const Container = styled.div``;
const Wrapper = styled.div``;
